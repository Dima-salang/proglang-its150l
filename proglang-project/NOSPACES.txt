# command line programimport sysclass HLInt:    error = False    def __init__(self, args):        self.args = args    def run(self):        if (len(self.args) == 1):            print("Usage: python HLInt.py <filename>")            sys.exit(1)        elif (len(self.args) > 2):            print("Usage: python HLInt.py <filename>")            sys.exit(1)        else:            self.run_file(self.args[1])    def run_file(self, filename):        self.scan_file(filename)    def scan_file(self, filename):        with open(filename, "r") as f:            self.clean_source(f.readlines())    def clean_source(self, source):        # remove newlines and spaces        cleaned_lines = []        for line in source:            indent = len(line) - len(line.lstrip(" \t"))            stripped = line.strip()            if not stripped:                continue            parts = stripped.split()            cleaned_content = " ".join(parts)            cleaned_line = (" " * indent) + cleaned_content            cleaned_lines.append(cleaned_line)        # write the new source to a file        with open("NOSPACES.txt", "w") as f:            f.writelines(cleaned_lines)    def scan_source(self, filename):        tokens = []    def error(self, message):        print(message)        self.error = Truedef main():    HLInt(sys.argv).run()if __name__ == "__main__":    main()